"use client";
import{Eye,EyeOff}from"lucide-react";import Link from"next/link";import{useRouter}from"next/navigation";import{FormEvent,useState}from"react";import{Logo}from"@/components/logo";
export default function Page(){const router=useRouter();const[reg,setReg]=useState(false);const[show,setShow]=useState(false);function submit(e:FormEvent){e.preventDefault();router.push("/notes")}return <main className="auth-page"><section className="auth-intro"><Logo/><div><span className="eyebrow">YOUR LEARNING, IN ONE PLACE</span><h1>学びをメモし、<br/>知識を育てる。</h1><p>気づきを残して整理する。仲間の学びに触れて、次の一歩につなげる。</p></div><small>© 2026 Manabi Note</small></section><section className="auth-panel"><form className="auth-form" onSubmit={submit}><div><h2>{reg?"アカウントを作成":"おかえりなさい"}</h2><p>{reg?"学びの記録を今日から始めましょう。":"アカウントにログインして続けましょう。"}</p></div>{reg&&<label>表示名<input required/></label>}<label>メールアドレス<input required type="email" placeholder="you@example.com"/></label><label>パスワード<span className="password-field"><input required type={show?"text":"password"} minLength={8}/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></span></label><button className="button primary auth-submit">{reg?"登録する":"ログイン"}</button><p className="auth-switch">{reg?"すでにアカウントをお持ちですか？":"アカウントをお持ちでないですか？"}<button type="button" onClick={()=>setReg(!reg)}>{reg?"ログイン":"新規登録"}</button></p><Link className="demo-link" href="/notes">モック画面を見る</Link></form></section></main>}
