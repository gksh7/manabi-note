import{ArrowLeft,Heart,Pencil}from"lucide-react";import Link from"next/link";import{notFound}from"next/navigation";import{CommentSection}from"@/components/comment-section";import{MarkdownPreview}from"@/components/markdown-preview";import{notes}from"@/lib/mock-data";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const n=notes.find(x=>x.id===id);if(!n)notFound();return <div className="article-wrap"><Link className="back-link" href="/notes"><ArrowLeft size={16}/>一覧へ戻る</Link><article className="article"><header><div className="article-title-row"><div><div className="tag-list">{n.tags.map(t=><span className="tag" key={t}>#{t}</span>)}</div><h1>{n.title}</h1></div><Link className="icon-button outlined" href={`/notes/${n.id}/edit`}><Pencil size={17}/></Link></div><div className="author-row"><span className="avatar small">{n.author[0].toUpperCase()}</span><span><strong>{n.author}</strong><small>更新 {n.updatedAt}</small></span><span className="status public">公開</span></div></header><MarkdownPreview>{n.body}</MarkdownPreview><footer><button className="button secondary"><Heart size={16}/>参考になった {n.likes}</button></footer></article><CommentSection/></div>}
