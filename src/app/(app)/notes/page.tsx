"use client";
import{Search,SlidersHorizontal}from"lucide-react";import Link from"next/link";import{useMemo,useState}from"react";import{NoteCard}from"@/components/note-card";import{notes,tags}from"@/lib/mock-data";
export default function Notes(){const[q,setQ]=useState("");const[tag,setTag]=useState("すべて");const visible=useMemo(()=>notes.filter(n=>n.isPublic&&(tag==="すべて"||n.tags.includes(tag))&&`${n.title} ${n.body}`.toLowerCase().includes(q.toLowerCase())),[q,tag]);return <div className="page-wrap"><div className="page-heading"><div><span className="eyebrow">COMMUNITY NOTES</span><h1>みんなのメモ</h1><p>誰かの学びが、次の発見につながります。</p></div><Link className="button primary" href="/notes/new">新しいメモ</Link></div><div className="toolbar"><label className="search"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="キーワードで検索"/></label><button className="icon-button outlined" aria-label="絞り込み"><SlidersHorizontal size={17}/></button></div><div className="filter-tabs">{["すべて",...tags].map(t=><button key={t} className={tag===t?"selected":""} onClick={()=>setTag(t)}>{t}</button>)}</div><section className="note-list">{visible.map(n=><NoteCard key={n.id} note={n}/>)}{!visible.length&&<div className="empty-state"><h2>メモが見つかりません</h2><p>検索条件を変えてみてください。</p></div>}</section></div>}
