# 学習メモ管理・共有アプリ 計画書

## 1. 目的

学習中に得た知識をメモとして蓄積し、タグで整理し、必要に応じて他のユーザーへ公開できるアプリを作る。
公開されたメモにはコメントを残せるようにし、学習内容を個人の記録だけでなく、他者との共有・振り返りにも使える状態にする。

## 2. 技術スタック

- フロントエンド: Next.js
- バックエンド: Supabase
- 認証: Supabase Auth
- データベース: Supabase PostgreSQL
- ファイル保存: 初期版では未使用。将来的に画像添付が必要になった場合は Supabase Storage を使う。
- デプロイ候補: Vercel

## 3. 想定ユーザー

- 学習内容を継続的に記録したい個人ユーザー
- 他の人の学習メモを参考にしたいユーザー
- 公開メモにコメントして質問・補足をしたいユーザー

## 4. 主要機能

### 4.1 ログイン機能

- メールアドレスとパスワードでアカウント登録・ログインできる。
- ログイン済みユーザーだけがメモの作成・編集・削除を行える。
- 公開メモを含むアプリ内のメモは、ログイン済みユーザーだけが閲覧できる。

### 4.2 メモ管理

- ログイン済みユーザーは学習メモを作成できる。
- 自分が作成したメモは編集・削除できる。
- メモには以下の情報を持たせる。
  - タイトル
  - 本文
  - タグ
  - 公開状態
  - 作成日時
  - 更新日時
  - 作成者
- 本文は Markdown で記述し、詳細画面で整形して表示する。

### 4.3 タグ分類

- メモには複数のタグを付けられる。
- タグは全ユーザー共通のマスターとして管理し、既存タグを再利用できるようにする。
- タグで自分のメモを絞り込める。
- 公開メモ一覧でもタグによる絞り込みを可能にする。

### 4.4 公開・非公開切り替え

- メモごとに公開・非公開を切り替えられる。
- 公開メモはログイン済みの全ユーザーが閲覧できる。
- 非公開メモは作成者本人だけが閲覧できる。

### 4.5 コメント機能

- 公開メモにはログイン済みユーザーがコメントできる。
- コメントは作成者、本文、作成日時を表示する。
- コメントの削除は、コメント投稿者本人またはメモ作成者のみ可能にする。
- 初期版ではコメント編集は必須にしない。

## 5. 画面構成

### 5.1 トップページ

- 公開メモ一覧を表示する。
- キーワード検索、タグ絞り込みを行える。
- ログイン済みユーザーだけが閲覧でき、未ログイン時はログインページへ移動する。

### 5.2 ログイン・登録ページ

- メールアドレスとパスワードでログインできる。
- 新規登録への切り替えを用意する。
- Supabase Auth のエラーを分かりやすく表示する。

### 5.3 マイメモ一覧ページ

- ログイン済みユーザー専用。
- 自分が作成したメモを一覧表示する。
- 公開・非公開の状態を確認できる。
- タグ、検索語、公開状態で絞り込める。
- 新規作成ページへ移動できる。

### 5.4 メモ作成・編集ページ

- タイトル、本文、タグ、公開状態を入力できる。
- 本文は Markdown で入力でき、プレビューで表示結果を確認できる。
- 作成時は新規保存、編集時は既存メモを更新する。
- 自分のメモ以外は編集できない。

### 5.5 メモ詳細ページ

- メモのタイトル、本文、タグ、作成者、更新日時を表示する。
- 公開メモはログイン済みの全ユーザーが閲覧できる。
- 非公開メモは作成者本人のみ閲覧できる。
- 公開メモにはコメント欄を表示する。

### 5.6 プロフィールページ

- ユーザー名を表示・編集できる。
- 公開メモの投稿者名として使う。
- 初期版ではプロフィール画像は任意または未対応とする。

## 6. データベース設計

### 6.1 profiles

Supabase Auth のユーザー情報に紐づくプロフィールテーブル。

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | uuid | Supabase Auth の user id。主キー |
| display_name | text | 表示名 |
| created_at | timestamptz | 作成日時 |
| updated_at | timestamptz | 更新日時 |

### 6.2 notes

学習メモ本体のテーブル。

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | uuid | 主キー |
| user_id | uuid | 作成者の user id |
| title | text | メモタイトル |
| body | text | メモ本文 |
| is_public | boolean | 公開状態 |
| created_at | timestamptz | 作成日時 |
| updated_at | timestamptz | 更新日時 |

### 6.3 tags

全ユーザーが共通で利用するタグのマスターテーブル。

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | uuid | 主キー |
| name | text | タグ名 |
| created_at | timestamptz | 作成日時 |

タグ名は重複を避けるため、一意制約を付け、ユーザーごとには分けない。

### 6.4 note_tags

メモとタグを結びつける中間テーブル。

| カラム | 型 | 説明 |
| --- | --- | --- |
| note_id | uuid | notes.id |
| tag_id | uuid | tags.id |

`note_id` と `tag_id` の組み合わせを主キーにする。

### 6.5 comments

公開メモへのコメントを保存するテーブル。

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | uuid | 主キー |
| note_id | uuid | コメント対象の note id |
| user_id | uuid | コメント投稿者の user id |
| body | text | コメント本文 |
| created_at | timestamptz | 作成日時 |
| updated_at | timestamptz | 更新日時 |

## 7. 権限設計

Supabase の Row Level Security を有効にし、データごとにアクセスできる条件を制御する。

### 7.1 notes

- 公開メモはログイン済みユーザーだけが閲覧できる。
- 非公開メモは作成者本人だけが閲覧できる。
- メモの作成はログイン済みユーザーのみ可能。
- メモの編集・削除は作成者本人のみ可能。

### 7.2 comments

- コメントの閲覧は、対象メモが公開されている場合のみ可能。
- コメントの作成はログイン済みユーザーのみ可能。
- コメントの削除は、コメント投稿者本人またはメモ作成者のみ可能。

### 7.3 profiles

- プロフィールは公開メモやコメント表示に必要な範囲で閲覧可能にする。
- プロフィールの更新は本人のみ可能。

### 7.4 tags

- タグの閲覧はログイン済みユーザーのみ可能にする。
- タグは全ユーザー共通とし、同名タグを重複登録できないようにする。
- ログイン済みユーザーはメモ保存時に既存タグを利用し、存在しないタグを追加できる。

## 8. Next.js の構成案

App Router を使う。

```text
app/
  page.tsx
  login/
    page.tsx
  notes/
    page.tsx
    new/
      page.tsx
    [id]/
      page.tsx
      edit/
        page.tsx
  profile/
    page.tsx
components/
  AuthButton.tsx
  CommentForm.tsx
  CommentList.tsx
  NoteCard.tsx
  NoteEditor.tsx
  TagInput.tsx
lib/
  supabase/
    client.ts
    server.ts
  validations/
    note.ts
    comment.ts
```

## 9. 実装ステップ

### ステップ0: プロジェクト初期化と GitHub への初回 push

- Next.js プロジェクトを App Router、TypeScript、ESLint を使う構成で初期化する。
- `.gitignore` を確認し、環境変数や秘密情報が Git の管理対象に入らないようにする。
- Git リポジトリを初期化し、既定ブランチを `main` にする。
- GitHub に新しいリポジトリを作成し、ローカルリポジトリの接続先として設定する。
- 初期状態を最初のコミットとして記録し、`main` ブランチへ push する。
- README にアプリの概要、使用技術、ローカルでの起動方法を記載する。

完了条件: GitHub 上の `main` ブランチに、起動可能な Next.js プロジェクトの初回コミットが存在する。

### ステップ1: アプリ全体のデザイン画像を作る

- トップ、ログイン・登録、マイメモ一覧、メモ作成・編集、メモ詳細、プロフィールの各画面を設計する。
- PC とスマートフォンのレイアウト、ナビゲーション、入力フォーム、一覧、コメント欄、空・読込中・エラー状態を含める。
- 色、文字、余白、ボタン、入力欄、タグ、公開状態の表示ルールを統一する。
- 全画面の完成イメージを確認できるデザイン画像を作成し、プロジェクト内の `docs/design/` に保存する。

完了条件: 実装時の見本として使える、主要画面と主要状態を網羅したデザイン画像がそろっている。

### ステップ2: デザイン画像どおりにフロントエンド全体のモックを作る

- デザイン画像をもとに、Next.js で全画面と共通コンポーネントを実装する。
- この段階では固定のサンプルデータを使い、Supabase にはまだ接続しない。
- 画面遷移、フォーム操作、検索・絞り込み、公開状態切り替え、コメント表示などを疑似的に操作できるようにする。
- PC とスマートフォンの双方でレイアウト崩れや文字の重なりがないことを確認する。
- デザイン画像とモックを見比べ、見た目と操作の差を調整する。

完了条件: バックエンドなしでも、利用者がアプリ全体の操作の流れを確認できる。

### ステップ3: Supabase につないで全機能を実装する

- Supabase プロジェクトを用意し、Next.js から安全に接続するための環境変数とクライアントを設定する。
- `profiles`、`notes`、`tags`、`note_tags`、`comments` のテーブル、制約、インデックスを作成する。
- Row Level Security と各ポリシーを設定し、非公開メモや他ユーザーのデータを保護する。
- 新規登録、ログイン、ログアウト、ログイン状態の保持、認証が必要なページの保護を実装する。
- Markdown の入力・安全なプレビュー・表示を含め、メモの作成・閲覧・編集・削除、共通タグの付与、検索・絞り込み、公開・非公開切り替えを実装する。
- 公開メモの一覧・詳細と、コメントの投稿・表示・削除を実装する。
- 入力チェック、エラー表示、読込中表示、空状態を実データに対応させる。

完了条件: 計画書の主要機能が Supabase の実データと認証を使って動作し、権限もデータベース側で守られている。

### ステップ4: Playwright で自動の動作確認をする

- Playwright を導入し、テスト専用ユーザーとテストデータの準備・後片付け方法を決める。
- 新規登録・ログイン・ログアウトの動作をテストする。
- メモの作成・編集・削除、タグ付け、検索・絞り込み、公開状態切り替えをテストする。
- ログイン後の公開メモ閲覧、Markdown 表示、コメント投稿・削除をテストする。
- 未ログインでは公開メモも閲覧できないこと、他ユーザーの非公開メモや編集画面にアクセスできないことをテストする。
- PC とスマートフォン相当の画面幅で主要な操作を確認し、失敗時にはスクリーンショットや記録を残す。
- 自動テストを継続的に実行できるよう、必要に応じて GitHub Actions に組み込む。

完了条件: 主要な利用経路と権限制御を自動テストで再現でき、すべてのテストが成功する。

### ステップ5: セキュリティを含めてコードレビューする

- 認証・認可、Row Level Security、公開・非公開の判定、コメント削除権限を重点的に確認する。
- Supabase の管理者用秘密鍵などがブラウザ側のコードや Git 履歴に含まれていないことを確認する。
- 入力値の検証、XSS、意図しない情報公開、エラー内容からの情報漏えい、過剰なデータ取得を確認する。
- Next.js の Server Components、Server Actions、Route Handlers の境界と、環境変数の扱いを確認する。
- 保守性、型安全性、重複、エラー処理、アクセシビリティ、表示速度もレビューする。
- 見つかった問題を重要度順に整理して修正し、Playwright のテストを再実行する。

完了条件: 重大・高優先度の指摘が解消され、修正後も自動テストがすべて成功している。

## 10. 入力チェック

- タイトルは必須、最大100文字程度。
- 本文は必須、最大10000文字程度。
- タグ名は最大30文字程度。
- コメント本文は必須、最大1000文字程度。
- 空白だけの入力は保存しない。

## 11. 検索・絞り込み

初期版では以下を実装する。

- 公開メモ一覧のキーワード検索
- マイメモ一覧のキーワード検索
- タグ絞り込み
- 公開状態による絞り込み

全文検索が必要になった場合は、Supabase PostgreSQL の全文検索機能を追加する。

## 12. UI 方針

- 学習に集中できる、落ち着いたシンプルな画面にする。
- メモ一覧ではタイトル、タグ、公開状態、更新日時がすぐ分かるようにする。
- 作成・編集画面は入力欄を広めに取り、長文メモを書きやすくする。
- 公開・非公開の切り替えは保存前に分かりやすく表示する。

## 13. 初期版でやらないこと

- 画像添付
- いいね機能
- フォロー機能
- 通知機能
- コメント編集
- 管理者画面

これらは初期版完成後に、利用状況を見ながら追加を検討する。

## 14. 将来的な拡張案

- 画像添付
- 公開メモへのいいね
- ユーザーのフォロー
- 人気タグ一覧
- 学習テーマごとのコレクション
- AI によるメモ要約
- AI による復習問題の自動生成

## 15. リスクと注意点

- 公開・非公開の権限設定を間違えると、非公開メモが見えてしまう可能性があるため、Row Level Security のテストを重視する。
- コメント機能は迷惑投稿の入口になりやすいため、将来的には通報・ブロック・削除管理の仕組みが必要になる。
- タグの表記ゆれが起きやすいため、保存時に前後の空白削除や小文字化などのルールを検討する。
- 本文が長くなる場合に備え、一覧表示では本文を短く省略する。

## 16. 完了条件

- ユーザー登録・ログイン・ログアウトができる。
- ログイン済みユーザーがメモを作成・編集・削除できる。
- メモ本文を Markdown で入力・プレビュー・表示できる。
- メモに全ユーザー共通のタグを複数付けられる。
- メモを公開・非公開に切り替えられる。
- 公開メモはログイン済みユーザーだけが閲覧できる。
- 非公開メモは作成者本人以外から閲覧できない。
- 公開メモにログイン済みユーザーがコメントできる。
- 権限設定が Supabase 側でも守られている。
